export const ADDMOB_SERVICE_MOCK = {
  // eslint-disable-next-line no-empty-function
  pushBanner(): void {},
  getBannerConfig(): any {
    return {
      id: '',
      isTesting: true,
      autoShow: true,
    };
  },
  // eslint-disable-next-line no-empty-function
  pushInterstitial(): void {},
  getInterstitialConfig(): any {
    return {
      id: '',
      isTesting: true,
      autoShow: true,
    };
  },
};
